package list.linkedlist.implementation;

public class Main {
	public static void main(String[] args) {
		LinkedList numbers = new LinkedList();
		
		//---------------------------------------------------------------------------
		// addFirst(Object)는 가장 첫번째 위치에 노드를 추가하는 메소드 입니다.
		numbers.addFirst(30);
		numbers.addFirst(20);
		numbers.addFirst(10);
	}
}
